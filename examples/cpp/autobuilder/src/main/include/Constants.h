#pragma once

#include <pathplanner/lib/auto/PIDConstants.h>

namespace swerve{
    // constexpr pathplanner::PIDConstants AUTO_TRANSLATION_CONSTANTS(0, 0, 0);
    // constexpr pathplanner::PIDConstants AUTO_ROTATION_CONSTANTS(0, 0, 0);
}
